{
  "homepage": "https://neutronas.github.io/deadlockchallenge",
  "name": "deadlockchallenge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "start": "next start",
    "lint": "biome check",
    "format": "biome format --write",
    "build": "cross-env NODE_ENV=production next build",
    "postbuild": "node scripts/postbuild.js",
    "deploy": "npm run build && npm run postbuild && gh-pages -d out"
  },
  "dependencies": {
    "next": "15.5.6",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "cross-env": "^10.1.0",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
